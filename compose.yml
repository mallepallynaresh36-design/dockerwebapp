version: "3.9"

services:
  devopsdb:
    image: nareshmallepalli006/dockerapp:v1
    ports:
      - "3306:3306"
    deploy:
      replicas: 3
    networks:
      - mynetwork

  application:
    image: nareshmallepalli006/dockerapp:v2
    ports:
      - "1111:8080"
    deploy:
      replicas: 3
    depends_on:
      - devopsdb
    networks:
      - mynetwork

networks:
  mynetwork:
    driver: overlay
